services:
  web:
    build:
      context: ./app
    ports:
      - "8000:8000"

  bot:
    build:
      context: ./bot
    depends_on:
      - web
    environment:
      BASE_URL: http://web:8000/
      FLAG: ${FLAG:-TRX{fake_flag_fake_flag_fake_flag_}}
    ports:
      - "9000:9000"
