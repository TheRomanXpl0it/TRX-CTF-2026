

# This file was *autogenerated* from the file solve.sage
from sage.all_cmdline import *   # import sage library

_sage_const_1 = Integer(1); _sage_const_0 = Integer(0); _sage_const_2 = Integer(2)
from pwn import *
import sys
import hashlib
from Crypto.Cipher import AES
from Crypto.Util.Padding import pad, unpad

url = sys.argv[_sage_const_1 ].split(":")
io = remote(url[_sage_const_0 ], url[_sage_const_1 ])

def send_point(io, p):
    io.sendlineafter(b"x: ", str(p[_sage_const_0 ]).encode())
    io.sendlineafter(b"y: ", str(p[_sage_const_1 ]).encode())
    io.sendlineafter(b"z: ", str(p[_sage_const_2 ]).encode())

def rot(io, p):
    io.sendlineafter(b"> ", b"1")
    send_point(io, p)
    s = io.recvline().decode()

    s = s[_sage_const_1 :-_sage_const_2 ]
    coords = s.split(',')
    coords = [SR(x) for x in coords]

    return vector(coords)

p1 = vector([_sage_const_0 , _sage_const_1 , _sage_const_0 ])
p2 = rot(io, p1)
p3 = rot(io, p2)

v1 = p2 - p1
v2 = p3 - p1
n = v1.cross_product(v2)
u = n / n.norm()


io.sendlineafter(b"> ", b"2")
send_point(io, u)
ct = bytes.fromhex(io.recvline().decode())

data = '0,0,0'
h = hashlib.sha256(data.encode()).digest()
aes = AES.new(h, AES.MODE_ECB)
pt = unpad(aes.decrypt(ct), AES.block_size)
print(pt)

io.close()


